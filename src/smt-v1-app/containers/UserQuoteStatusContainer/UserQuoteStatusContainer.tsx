import React from 'react';
import UserQuoteStatus from 'smt-v1-app/components/features/UserQuoteStatus/UserQuoteStatus';

const UserQuoteStatusContainer: React.FC = () => {
  return (
    <div className="user-quote-status-container">
      <UserQuoteStatus />
    </div>
  );
};

export default UserQuoteStatusContainer;
